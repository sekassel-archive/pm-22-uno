package de.uniks.pmws2223.uno.service;

import org.junit.Test;

public class BotServiceTest {
    @Test
    private void checkCardsForPlayable() {
        //setup work
        //bot has a playable card and type is set to null
        //bot doesn't have a playable card and type is set to null
        //bot has a playable card and type is set to drawtwo
        //bot doesn't have a playable card and type is set to drawtwo
    }

    @Test
    private void wishBotColor() {
        //setup work
        //assert that bot chooses the color with the higest cardcount
        //assert that bot chooses yellow if it has the tied highest card count
    }
}
